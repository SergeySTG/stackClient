import React, { FC, ReactElement } from 'react';
import './question.styles.scss';

export const QuestionPage: FC = (): ReactElement => {
  return <div className="question-page">Question page</div>;
};
